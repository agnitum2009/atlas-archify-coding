# Contributing

感谢参与。本仓由上游内部工作仓**机械投影**生成，因此贡献流程与普通仓略有不同 —— 目标是让你的改动
**活在下一次投影里**，而不是被覆盖。

## PR 会怎么被处理

1. 你在 `atlas-archify-coding` 开 PR，CI（`npm test` + 契约保鲜 + 隐私门禁）必须全绿；
2. 维护者评审后合并 —— **你的提交与署名永久留在本仓历史**；
3. 合并后，改动会被回流到上游实现仓（以补丁形式、保留作者署名），因此：
   - 若你的 PR 修改了**投影管辖的文件**（`lib/`、`bin/`、`specs/`、`test/`、`scripts/`），
     在下一次上游投影前，你的改动会处于"待回流"状态；维护者若未及时处理，下次投影会**报错停止而不是覆盖你**。
   - 纯文档/新增文件类 PR 冲突风险最低。

## 什么会被拒

- **新增命令 / 旗标 / 错误码 / 依赖**：需先过能力五问（是否服务"中后期进度掌控"、命名是否中性、
  是否可用现有命令组合达成、是否可机器验收、是否可退役）。预算是硬顶门禁，加一个就得减一个。
- 修复实现而不动契约文本：欢迎，最省事。
- 与 `specs/command-contract.md` 不一致的 --help 文案：CI 会直接拦下（这是有意的）。

## 提交约定

- 每个 PR 单一意图；commit message 说清"为什么"，行为变更须同步 `specs/`。
- 提交需带签名：`git commit -s`（DCO，声明你有权贡献且同意 MIT 许可）。
- 零依赖是硬约束：不要引入 npm 依赖（脚本层用 `node:` 内置）。

## 报安全问题

请勿开公开 issue，见 `SECURITY.md`。

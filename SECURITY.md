# Security Policy

## 报告渠道

请不要通过公开 issue 报告安全问题。发邮件到 **agnitum@gmail.com**，
我们会尽量 72 小时内回复。

## 本仓的信任模型（先读这段，能省你一次误报）

aac 是一个**本地文件账本工具**：它假设运行者对指定路径本就有读写权限，
不提供加密、不提供访问控制、不做多租户隔离。因此以下情况**不属于安全问题**（欢迎报告它们之外的真实风险）：

- 能读到你侧车文件的人能改它（`atlas-state.json` 明文 + CAS 版本号只防并发误覆盖，不防恶意）；
- `--sidecar` 指向任何路径都会被写入（工具的设计是"只处理显式传入"，不做路径猜探）；
- 证据锚记录的文件路径/行内容哈希会泄露被锚定文件的部分内容 —— 这本来就是审计目的。

会受理的真问题示例：外部可控数据能拼进 `exec`/`spawn` 参数、回执 JSON 注入、路径遍历逃逸出调用方
显式给定的根；以及任何让**账本看起来比实际更可信**的缺陷（例如非法状态迁移被放行、证据已漂移却仍返回通过）。

时间线：72 小时内回复；修复发布前请避免公开披露。若你希望走加密渠道，回信时说明即可。

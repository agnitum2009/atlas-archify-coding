# Security Policy

## 报告渠道

请不要通过公开 issue 报告安全问题。发邮件到 **<在此填写你的安全联系邮箱>**，
我们会尽量在 72 小时内回复。

## 本仓的信任模型（请先读完再报"漏洞"）

aac 是一个**本地文件账本工具**：它假设运行者对指定路径本就有读写权限，
不提供加密、不提供访问控制、不做多租户隔离。因此以下**不视为安全问题**：

- 能读到你侧车文件的人能改它（`atlas-state.json` 明文 + CAS 版本号只防并发误覆盖，不防恶意）；
- `--sidecar` 指向任何路径都会被写入（工具的设计是"只处理显式传入"，不做路径猜探）；
- 证据锚记录的文件路径/行内容哈希会泄露被锚定文件的部分内容 —— 这本来就是审计目的。

真安全问题（会受理）：命令执行类输入未做隔离、`exec`/`spawn` 参数拼接可被外部数据注入、
回执 JSON 注入、路径遍历逃逸出显式给定根。
